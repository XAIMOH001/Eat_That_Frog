# The journal is one person's private log and the auth endpoints are not content. Only the
# public marketing surface is crawlable — which today is just `/`.
User-agent: *
Disallow: /journal
Disallow: /sign-in
Disallow: /sign-up
Disallow: /api/
Allow: /
