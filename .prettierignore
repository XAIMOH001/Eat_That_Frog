node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts

# Generated by drizzle-kit. The .sql files are hand-edited but the meta/ snapshots are
# immutable history that prettier would otherwise reformat on every run, fighting the
# generator and churning files that must not change.
drizzle/meta

# Rewritten by Lovable and by `next dev`. Prettier reformats their managed blocks, which
# those tools then undo — pure churn either way.
AGENTS.md
